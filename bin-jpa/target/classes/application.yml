server:
  port: 8001
spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: -1
        max-idle: 500
        max-wait: -1
        min-idle: 50
    timeout: 1000
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/springboot-group?serverTimezone=UTC&useSSL=false
    username: root
    password: root
jwt:
  secret: binbin
  expiration: 180